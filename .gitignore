# data (CERT r4.2 is licence-restricted; never commit it)
# data/raw is a machine-specific symlink to the external disk; never commit it
data/raw
data/processed
*.parquet
*.csv.gz

# model artefacts
models/*
!models/.gitkeep
*.pt
*.pkl
*.joblib

# secrets and keys
.env
*.pem
keys/

# demo scenario is derived from the licence-restricted corpus
demo/*.parquet

# PDP runtime state: keys, local ledger, off-chain evidence store
state/
evidence/
*.jsonl

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# fabric
chaincode/**/vendor/
fabric-samples/

# os / editor
.DS_Store
.idea/
.vscode/
