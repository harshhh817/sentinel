# ZTBAudit — pinned dependencies (Python 3.11)
# Versions follow Table IV of the paper: PyTorch 2.1, scikit-learn 1.4.

# --- core numerics / data ---
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0            # parquet for data/processed/
scipy==1.13.1

# --- risk engine (Section IV-C) ---
torch==2.1.2               # deep autoencoder 34-24-16-8
scikit-learn==1.4.2        # IsolationForest(n_estimators=200, max_samples=256)
joblib==1.4.2              # model persistence

# --- PDP service (Section IV-A, Algorithm 1) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
cryptography==42.0.8       # ECDSA P-256 signing, evidence-store encryption

# --- ledger client (Section IV-E) ---
grpcio==1.64.1             # Fabric Gateway transport
protobuf==4.25.3

# --- results / figures ---
matplotlib==3.9.0

# --- tooling ---
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.0              # FastAPI TestClient
ruff==0.4.10

# --- demo dashboard (Module 3b) ---
streamlit==1.38.0
shap==0.46.0
plotly==5.24.1
